<%= form_for @post do |form| %>
  <%= form.text_area :title %>
  <%= form.text_area :tag_list, placeholder: "Separate tags with comma" %>
  <%= form.hidden_field :picture_cache %>
  <%= form.file_field :picture %>
  <%= form.submit "save" %>
<% end %>
